# mwanachama-backend-assetmanager — completed tasks

| Task | Title | Completed | Notes |
|------|-------|-----------|-------|
| A1 | Bootstrap repo: `git init`, `go.mod`, `.gitignore`, `Makefile`, four-phase `documentation/` skeleton, `README.md`, `CLAUDE.md`, `todo.md`/`todo_done.md` | 2026-09-03 | New repo, sibling of [mwanachama-backend-taskmanager](../../../mwanachama-backend-taskmanager), same conventions. Depends on [mwanachama-backend-shared](../../../mwanachama-backend-shared) for `entitygraph.DataManager` (wired via `go.mod` local `replace`, module `github.com/aosanya/mwanachama-backend-assetmanager`). Standalone product — not part of the Mwanachama party platform's tier/RLS model, no DSN board. |
| A2 | Write `1. requirements/requirements.md` + `2. design/architecture.md`: the 8-decision design session (product scope, schema shape, ledger-first Movement, multi-tenancy via `entitygraph`'s existing `AgencyID`, dual serialized/fungible Asset identity, recursive `Location` tree, stateful Hold with expiry + partial fulfillment) | 2026-09-03 | Research session's output — see requirements.md's decision table for the full record and rationale. Deliberately designed so the ledger/hold shapes are compatible with eventually absorbing the party's existing merchandise domain (`merchandise-entry`/`handout`/`merchandise-dispute`), without committing this repo to that domain's tier/RLS coupling. |
| A3 | Write `schema.go` (`DefaultAssetSchema`): Asset/Location/Movement/Hold `TypeDefinition`s | 2026-09-03 | Initial draft (same session, scaffold-only) declared an edge per cross-reference (`located_at`, `of_asset`, `from_location`, `to_location`, `reserves`, `at_location`) mirroring taskmanager's `assigned_to`-style edges. **Revised during A6/A7 implementation**: none of those references are ever walked multi-hop, so they were collapsed to plain denormalized string properties (`location_id`, `asset_id`, `from_location_id`, `to_location_id`) — the same choice taskmanager made for `Task.WorkflowRunID` ("denormalises the edge onto the row so queries can filter without traversing the graph"). The **one** relation kept as a real edge is `Location`'s `child_of` — the only reference this schema ever needs to walk multi-hop (`ListDescendantLocations`'s recursive-tree query). Renamed from the scaffold's `parent_of` to `child_of` to match taskmanager's `subtask_of` convention (edge reads "X is child_of Y", source is the child). **Postgres caught a real bug the fake-backed unit tests couldn't**: `entitygraph.ValidateSchema` requires *both* directions of a relationship with an `Inverse` to be real declared `RelationshipDefinition`s (see taskmanager's `blocked_by`/`has_subtask`/`depended_on_by` pairs) — `child_of`'s declared inverse `has_child_location` didn't exist as its own relationship, and `Publish` rejected the schema. Fixed by adding `RelLabelHasChildLocation` as `Location`'s second, symmetric `RelationshipDefinition`. Caught by A8's Postgres integration run, not by any unit test — the fake's `TraverseGraph`/schema handling never calls `ValidateSchema` at all. |
| A4 | Write `models.go`: `Location`/`Asset`/`Movement`/`Hold` structs, `AssetTrackingMode`, `MovementKind`, `HoldStatus.CanTransitionTo` | 2026-09-03 | String-timestamp (RFC 3339) convention throughout, matching taskmanager. `HoldStatus.CanTransitionTo` only allows transitions out of `reserved` — every outcome (`committed`/`released`/`partially_committed`) is terminal, encoding the single-shot commit semantics requirements.md decided (§8): `CommitHold` is called at most once per hold. |
| A5 | Write `errors.go`, `converters.go`, `asset_manager.go` (`AssetManager` interface, `assetManager` struct, `NewAssetManager` constructor, `LocationFilter`/`AssetFilter`/`MovementFilter`/`HoldFilter`) | 2026-09-03 | Mirrors taskmanager's `errors.go`/`task_impl_converters.go`/`task.go` split exactly — sentinel errors wrapped with `fmt.Errorf("%w: ...")`, `entitygraph.StringProp`/`Int64Prop` used directly in converters, every `AssetManager` method takes `agencyID` explicitly (this backend is multi-tenant by design, §4). No event `Publisher` — not part of the agreed design scope, unlike taskmanager's optional `events.Publisher`. |
| A6 | Write `location.go`: Location CRUD + `child_of` tree (`CreateLocation` with atomic inline-relationship creation, `UpdateLocation` re-parenting with cycle rejection, `DeleteLocation` refusing non-empty locations, `ListDescendantLocations` via `TraverseGraph`) | 2026-09-03 | Re-parenting cycle check (`reparentLocation`) walks the candidate's own descendant set before accepting a new parent — proven against a real 3-level tree in A8's Postgres suite, not just the fake's single-hop case. `DeleteLocation` refuses when the location has child locations *or* assets currently denormalized to it (`ErrLocationNotEmpty`) rather than silently orphaning either. |
| A7 | Write `asset.go`, `movement.go`, `hold.go`, `hold_watchdog.go`: Asset CRUD, the append-only Movement ledger (`PostMovement`/`ReverseMovement`/`GetAssetBalance`), and the Hold reservation lifecycle (`CreateHold`/`CommitHold`/`ReleaseHold`/`ListHoldsExpiredAsOf`) | 2026-09-03 | **Movement**: `PostMovement` validates one of four shapes per `Kind` (arrived/transferred/departed/adjusted — `reversed` is refused, only `ReverseMovement` may create one), enforces exactly-1 quantity for a serialized Asset, and patches `Asset.location_id` as a best-effort denormalization in a second, non-atomic `DataManager` call — the same cross-call-transaction gap taskmanager's `WorkflowRun` rollback (W6) documents and defers, flagged the same way here rather than silently assumed away. `ReverseMovement` posts a mirror with `From`/`To` swapped (works for every `Kind` including signed `adjusted` entries — the swap cancels the fold to zero regardless of sign) and refuses double-reversal and reversing a reversal. **Hold**: `CreateHold` checks real availability (balance minus every other open hold's reserved quantity at that location) before accepting, matching the "don't oversell" requirement from requirements.md §7-8; `CommitHold` is single-shot per the state machine (A4) — quantity below the held amount auto-releases the remainder in the same call. `ListHoldsExpiredAsOf` is a query-only watchdog helper, same split as taskmanager's `workflow_run_watchdog.go` — no scheduler in this package, the sweep loop is left to whatever wires this in. |
| A8 | Unit tests (`fake_test.go` + `location_test.go`/`asset_test.go`/`movement_test.go`/`hold_test.go`, 36 tests) and Postgres integration tests (`postgres_integration_test.go`, 4 tests) | 2026-09-03 | **Unit tests** run against a `fakeDataManager` modelled on taskmanager's own (in-memory, single-hop `TraverseGraph` — not schema-aware, so it never calls `ValidateSchema`). Cover every CRUD error path, the full ledger fold across arrived/transferred/departed/adjusted/reversed (including a negative `adjusted` correction and reversal-cancels-to-zero), the serialized-quantity-must-be-1 invariant, one-hop cycle rejection on re-parenting, and the full Hold lifecycle (insufficient-availability rejection, full/partial commit, release, and the expiry-watchdog query). **Postgres integration tests**, mirroring taskmanager's own split (`POSTGRES_URL`-gated, skipped without a database) — **actually run this session** against the `mwanachama-test-pg` scratch container already up on `localhost:55433` (not just written and left unverified): 4 tests covering Location/Asset CRUD round-trip, the Movement ledger fold across two locations, a **3-level** `ListDescendantLocations` (warehouse → aisle → bin) proving the real recursive-CTE traversal the fake's single-hop version can't honestly test, a multi-hop re-parent-cycle rejection, and the full Hold commit lifecycle. This run is what caught A3's `ValidateSchema` bug — `go test ./...` alone (fake-backed) would have shipped a schema `SchemaManager.Publish` rejects on first real use. All 40 tests pass; `go build ./...`, `go vet ./...`, `gofmt -l .`, `go test -race ./...` all clean. |

## Archived board context

`mwanachama-backend-assetmanager` — Go library, module
`github.com/aosanya/mwanachama-backend-assetmanager`. Standalone —
consumed by whatever wires it in; not yet wired into any specific caller.

### Why this repo exists

The user asked for "a similar backend" to
[mwanachama-backend-taskmanager](../../../mwanachama-backend-taskmanager) for
general asset/inventory tracking — warehouses, supermarkets, household
inventories, livestock — reusing the same `entitygraph.DataManager`
architecture. Confirmed mid-session it should eventually be able to back
the Mwanachama party's existing merchandise domain too, which shaped the
ledger-first, hold-capable design in `1. requirements/requirements.md`
without coupling this repo to that domain's tier/RLS model.
